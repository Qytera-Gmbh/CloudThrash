gatling {
  data {
    writers = ["console", "file", "graphite"]

    graphite {
      host = "graphite.loadtest"
      port = 2003
      protocol = "tcp"
      rootPathPrefix = "gatling"
      bufferSize = 8192
      writeInterval = 1 second
    }
  }
}
